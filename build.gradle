group 'sample'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'maven'

sourceCompatibility = 1.8

def digdagVersion = '0.9.16'

repositories {
    mavenCentral()
    jcenter()
    maven {
        url 'https://dl.bintray.com/digdag/maven'
    }
}

configurations {
    provided
}

dependencies {
    compile 'io.digdag:digdag-spi:0.9.9'
    compile 'io.digdag:digdag-plugin-utils:0.9.9'
    compile group: 'com.amazonaws', name: 'aws-java-sdk-ecs', version: '1.11.192'
}
